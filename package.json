{
  "name": "todo-apollo-vue",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/husky"
    ]
  },
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"yarn workspace client serve\" \"yarn workspace server dev\""
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
